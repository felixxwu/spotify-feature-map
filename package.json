{
  "name": "spotify-with-tags",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc && vite build",
    "preview": "tsc && vite build && vite preview"
  },
  "dependencies": {
    "@preact/signals": "^2.0.1",
    "firebase": "^11.2.0",
    "goober": "^2.1.16",
    "preact": "^10.25.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.3",
    "typescript": "~5.6.2",
    "vite": "^6.0.5"
  }
}
